include:
  - "/.gitlab/dotnet/build-test.yml"
  - "/.gitlab/mockoon/demo.yml"

ci:
  stage: build
  image: node:lts
  script:
    - npm install -g markdownlint-cli
    - markdownlint README.md

stages:
  - build
  - test
